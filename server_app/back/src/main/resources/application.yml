spring:
  application:
    name: back
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://aivle-bigp-18-db.postgres.database.azure.com:5432/postgres
    username: aivlePostgres@aivle-bigp-18-db
    password: ssss1185!
    driver-class-name: org.postgresql.Driver
  mail:
    host: smtp.naver.com
    port: 587
    username: ${MAILUSER}
    password: ${MAILPASS}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: false

logging:
  level:
    root: DEBUG

jwt:
  secret: ${JWT_SECRET}

admin:
  id: ${ADMIN_ID}
  password: ${ADMIN_PASS}

ai:
  protocols: ${AI_PROTOCOLS}
chatai:
  host: ${CHATAI_HOST}
  port: ${CHATAI_PORT}